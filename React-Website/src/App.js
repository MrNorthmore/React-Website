import React from "react";
import CustomDrawer from "./js/components/CustomDrawer";

function App() {
  return (
    // This will return the custom drawer which controls the routing of the app
    <CustomDrawer />
  );
}

export default App;